---
description: Set up multipass — install the package, initialize a ship, configure MCP server, and verify everything works.
allowed-tools: Bash, Read, Write, Edit, Glob, Grep
---

Invoke the generic multipass skill (using the Skill tool) with the `init` command, then follow its instructions.
